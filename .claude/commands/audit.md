---
description: Fresh-context citation check. Run before anything is published.
---
You did not write this draft. Do not defend it.

Target: $ARGUMENTS — if empty, today's lesson file, today's newsletter draft
and any wiki page changed today.

For each:

1. Open **every** citation. Confirm the linked page exists, is dated, and
   actually contains the claim attributed to it.
2. List every claim with no source, and every source that does not support
   its claim. Quote the sentence and the source line.
3. Check no piece predates a source it cites.
4. Check the tier is right. The tier describes **the URL**, not the
   organisation behind the data: a survey reached through a news article is
   `reported`, publisher the outlet.

Report findings only. Fix nothing. If everything passes, say so in one line —
do not manufacture a finding to look useful.
