---
description: The 06:00 block. Research today's session, write the lesson file, open a PR.
---
Follow `AI101/prompts/daily-research.md` exactly.

Day number: $ARGUMENTS — if empty, work it out by counting files in
`content/lessons/` and add one.

Do not summarise the prompt back to me. Run it, write
`content/lessons/<today>.md`, append anything offline to `TASKS.md`, and open
a pull request. The PR body is the angle and the five beats, nothing more —
I read it on a phone.
