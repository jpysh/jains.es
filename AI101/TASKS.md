# Tasks

Things only you can do. One line each. Delete when done, don't tick.

## Before the fresh session

- [ ] **Paste your AI-news sources** into the `news` block of
  `AI101/sources/SOURCES.md`. One per line, same format as the other blocks.
  Anything with an RSS feed beats anything without. This is the single blocker
  for the scout doing useful work.
- [ ] **Paste your existing newsletter prompt** into the top of
  `AI101/prompts/newsletter.md`, verbatim, however rough. I'll merge what works
  rather than replace it.
- [ ] **Drop the subscriber CSV** into `/Users/webserver/code/private/list/`.
  It stays there permanently — not mailed, not committed. Do this so it stops
  being a loose end, not because anything depends on it.
- [ ] **Confirm you can push to `github.com/jpysh/jains.es`** — run
  `git -C ~/code/jains.es push --dry-run` and make sure it doesn't prompt.
  The nightly job needs this to work unattended later.

## YouTube — 15 minutes

- [ ] **Unlist all 7 videos on @doctorjapa.** Studio → Content → select all →
  Visibility → Unlisted. Don't delete; unlisting resets the topic signal
  without losing anything.
- [ ] **Rewrite the channel description.** Currently "talent and content
  discovery". Make it what this is now — one sentence, plain, no hype.
- [ ] **Set the channel's display name** to match whatever you want on the
  newsletter and LinkedIn. Handle stays `@doctorjapa`.

## Accounts — 20 minutes, all free, no cards

- [ ] **Kaggle** — verify your phone number. GPU access is gated on it and you
  don't want to discover that in week 5.
- [ ] **Modal** — sign up for Starter. $30/month in credits, renewable, no card.
- [ ] **Google AI Studio** — get an API key. Check your actual rate limits at
  `aistudio.google.com` rather than trusting any published table.
- [ ] **Kit** — create the account, free tier. Don't import anything.

## Day 1, tomorrow, 06:00 Madrid

- [ ] Open Claude Code, paste `AI101/prompts/daily-research.md` with `day 1`.
- [ ] Session: count tokens for the same sentence in English, Hindi, Tamil and
  Bengali with `tiktoken`. Tabulate the ratios. That's the whole lesson.
- [ ] Record it vertical on the phone, Apple wired mic, five beats. Beat 4 is
  "the part I am least sure about" — it always is.
- [ ] Publish. Don't wait for infrastructure.

## Not urgent, but don't lose it

- [ ] Decide whether the two advisor slots (Kavyanjali Kaushik, Manish Jain)
  get asked. Nothing goes on the site until they've said yes.
- [ ] Find any archive of excelmindsjobs.com — Wayback, a local backup, an old
  export. Not for mailing; it's the only surviving record of what you told
  those people, and it may matter later.
