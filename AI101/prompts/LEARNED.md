# Learned

Corrections from `/daily`, one line each. Every run reads this file, so keep it
short: once a month, move each repeated rule into the prompt it belongs to and
delete the rest.

Format: `DATE | step | what was wrong, and what to do instead.`

<!-- entries below, newest last -->
